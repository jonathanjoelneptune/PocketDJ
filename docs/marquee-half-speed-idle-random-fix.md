# Pocket DJ Marquee Half-Speed and Idle Random Fix

<span style="color:#16a34a;">Changed:</span> Marquee transition in/out speed is roughly half the prior speed.

<span style="color:#16a34a;">Changed:</span> Artist line no longer does the extra drift/scroll while the title is fixed.

<span style="color:#16a34a;">Changed:</span> Idle now randomly chooses a calm idle pose when entering/changing idle state instead of stepping through the list.

<span style="color:#dc2626;">Removed:</span> `i8` from the idle and paused animation pools.
