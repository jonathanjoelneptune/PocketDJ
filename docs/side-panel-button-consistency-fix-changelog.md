# Pocket DJ Side Panel Button Consistency Fix Changelog

<span style="color:#16a34a;">Changed:</span> `IDLE` / `PLAYING` and `Connected` pills now use the same sizing and text scale as the SHUF/REP panel buttons.

<span style="color:#16a34a;">Changed:</span> Reduced the extra vertical space between the top row and the now-playing block.

<span style="color:#16a34a;">Changed:</span> Side-panel play/pause, previous, next, shuffle, and repeat buttons now use one consistent button footprint.

<span style="color:#16a34a;">Changed:</span> Play/pause remains visually emphasized by fill color, not by odd button sizing.

<span style="color:#16a34a;">Changed:</span> Disconnect, Demo, Debug, Room Utility, and other dev buttons now share the same compact button sizing system.

<span style="color:#16a34a;">Changed:</span> Volume row spacing tightened so the panel reads as one clean playback module.
