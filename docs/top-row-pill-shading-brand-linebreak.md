# Pocket DJ Top-Row Pill Shading and Brand Line Break

<span style="color:#16a34a;">Changed:</span> `Pocket DJ` now renders as two lines with `DJ` below `Pocket` so it is not covered in the compact top row.

<span style="color:#16a34a;">Changed:</span> The active `PLAYING` / `IDLE` pill and the enabled `LYRICS` pill now use the same warm connected-pill shading.

<span style="color:#16a34a;">Changed:</span> Lyrics status remains distinct by border color: green when found, red when unavailable/error/instrumental, and orange while searching.

<span style="color:#16a34a;">Changed:</span> The lyrics-off state remains dimmer and does not use the connected active shading.
