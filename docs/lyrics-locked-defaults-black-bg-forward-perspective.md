# Pocket DJ Lyrics Locked Defaults, Black Background, and Forward Perspective

<span style="color:#16a34a;">Changed:</span> Locked in the latest tuned lyric defaults from the user screenshot.

<span style="color:#16a34a;">Changed:</span> Active lyric default Y is 88 px, height is 25 px, opacity is 1.00, and background color is black.

<span style="color:#16a34a;">Changed:</span> Future lyrics section defaults are now top `(932, 293, 860)` and bottom `(882, 397, 900)`.

<span style="color:#16a34a;">Changed:</span> Past lyrics section defaults are top `(881, 42, 1177)` and bottom `(882, 168, 920)`.

<span style="color:#16a34a;">Fixed:</span> Active lyric background no longer becomes transparent when black is selected.

<span style="color:#16a34a;">Fixed:</span> Active lyric black stroke remains visible through `-webkit-text-stroke` and fallback text shadow/drop-shadow.

<span style="color:#16a34a;">Added:</span> Ceiling perspective selector with `Forward toward viewer` and `Star crawl away`.

<span style="color:#16a34a;">Changed:</span> Default ceiling perspective is now `Forward toward viewer`, which flips the ceiling projection away from the Star Wars crawl direction.

<span style="color:#16a34a;">Changed:</span> Room utility storage key bumped to `pocketdj-room-utility-v6` so stale v5 lyric geometry does not override the locked defaults. Older speaker and room filter settings still migrate.
