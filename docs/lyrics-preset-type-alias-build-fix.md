# Pocket DJ Lyrics Preset Type Alias Build Fix

<span style="color:#16a34a;">Fixed:</span> Added the missing `LyricAnimationPreset`, `ActiveLyricPreset`, and `InactiveLyricPreset` type aliases in `src/main.ts`.

<span style="color:#16a34a;">Fixed:</span> Resolves the TypeScript build errors where those lyric preset types were referenced but not declared.
