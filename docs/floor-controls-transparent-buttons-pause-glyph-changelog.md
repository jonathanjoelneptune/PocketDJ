# Pocket DJ Floor Controls Transparent Buttons and Pause Glyph Changelog

<span style="color:#16a34a;">Changed:</span> Floor playback button backgrounds are more transparent so the floor/room remains visible underneath.

<span style="color:#16a34a;">Changed:</span> Bottom-left `Controls` toggle is also more transparent while keeping the marquee amber glow.

<span style="color:#16a34a;">Changed:</span> Pause icon now uses clean `||` vertical bars instead of the roman-numeral-style `Ⅱ` glyph.

<span style="color:#16a34a;">Changed:</span> Pause icon letter spacing was tightened so the two bars read as a playback pause symbol.
